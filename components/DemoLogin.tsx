'use client';
import { useState } from 'react';

/** Development-only credentials. Never use this frontend-only account in production. */
export const DEMO_CREDENTIALS={username:'testcandidate',password:'Test@12345'} as const;
export default function DemoLogin({onSuccess}:{onSuccess:()=>void}){const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=()=>{if(username===DEMO_CREDENTIALS.username&&password===DEMO_CREDENTIALS.password)onSuccess();else setError('Use the development demo credentials shown below.');};return <main className="mx-auto max-w-md px-5 py-12"><div className="card p-7"><p className="text-xs font-bold text-amber-800">DEVELOPMENT / DEMO ACCOUNT</p><h1 className="mt-2 text-2xl font-bold">Sign in to your test</h1><p className="mt-2 text-sm text-slate-600">This account is for prototype testing only.</p><label className="mt-5 block text-sm font-bold">Username<input className="field" value={username} onChange={event=>setUsername(event.target.value)} autoComplete="username"/></label><label className="mt-4 block text-sm font-bold">Password<input className="field" type="password" value={password} onChange={event=>setPassword(event.target.value)} autoComplete="current-password"/></label>{error&&<p className="mt-3 text-sm font-bold text-red-700">{error}</p>}<button onClick={submit} className="btn-primary mt-6 w-full">Continue to candidate dashboard</button><div className="mt-5 rounded-xl bg-blue-50 p-4 text-sm"><b>Demo credentials</b><br/>Username: testcandidate<br/>Password: Test@12345</div></div></main>}
