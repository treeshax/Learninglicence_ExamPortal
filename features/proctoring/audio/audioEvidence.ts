const DB='ll-proctor-audio-evidence';
export async function storeAudioChunk(sessionId:string,blob:Blob){try{const db=await new Promise<IDBDatabase>((resolve,reject)=>{const request=indexedDB.open(DB,1);request.onupgradeneeded=()=>request.result.createObjectStore('chunks',{keyPath:'id'});request.onsuccess=()=>resolve(request.result);request.onerror=()=>reject(request.error);});const transaction=db.transaction('chunks','readwrite');transaction.objectStore('chunks').put({id:crypto.randomUUID(),sessionId,timestamp:Date.now(),mimeType:blob.type,blob});}catch{/* Evidence persistence must not interrupt the candidate's exam. */}}
